using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WrestlerManager : MonoBehaviour
{
    public List<string> listName = new List<string>();
      
    // Start is called before the first frame update
    void Start()
    {

        string[] firstNames = { "Barry", "Larry", "Steve", "Rick", "Greg" };
        string[] lastNames = { "Heart", "grace", "Jackson", "Valdez", "Ramsy" };
        string[] wrestlerNames = {};
    }


    // Update is called once per frame
    void Update()
    {
        
    }
}
